plugins {
    id "com.moowork.node" version "1.3.1"
}

npm_run {
    args = ['dev']
}

task run {
    dependsOn 'npm_run'
}

task build {
    dependsOn (['npm_install', 'npm_run_build'])
}

npm_run_build {
    args = ['--production']
}